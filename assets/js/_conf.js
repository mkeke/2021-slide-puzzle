/*
    _conf.js

    configurable values
*/
const conf = {
    // screen max/min width including padding
    minWidth: 380,
    maxWidth: 800,

    grids: [3, 4, 5, 6],
    defaultGrid: 5,

    // define white space on each side of the board
    hSpace: 10 + 2 + 2 + 10, // padding + border

    // define white space above and below board
    vSpace: 10 + 2 + 2 + 5 + 30 + 10, // padding, border, option icon
};
